# General pip Commands
```sh
# pip Upgrade
$ pip install --upgrade pip

# Install Package
$ pip install PACKAGE

# Create Requirements File (list Python dependencies)
$ pip freeze > requirements.txt

# Install Python Packages from Requirements
$ pip install -r requirements.txt

```
