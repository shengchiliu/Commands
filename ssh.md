# Basic SSH Commands

```sh
$ ssh USERNAME@IP_ADDRESS                       # access a remote system
$ ssh -Y USERNAME@IP_ADDRESS                    # using graphical applications remotely
$ ssh -p PORT_NUMBER USERNAME@IP_ADDRESS        # access a remote system via a specific port
```
